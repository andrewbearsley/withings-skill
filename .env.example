# Withings Developer App credentials
# Found at: https://developer.withings.com/dashboard/ > Your app
WITHINGS_CLIENT_ID=your_client_id
WITHINGS_CLIENT_SECRET=your_client_secret

# Path to store OAuth tokens (default: ~/.withings-tokens)
# The file is created with chmod 600 for security.
WITHINGS_TOKEN_FILE=~/.withings-tokens

# OAuth redirect URI -- must match what's registered in your Withings developer app
# WITHINGS_REDIRECT_URI=http://localhost:9876/callback
